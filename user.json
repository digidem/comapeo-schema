{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "?????",
  "title": "user",
  "description": "",
  "type": "object",
  "properties": {
    "prevIds": {
      "type": "array",
      "description": "IDs of user IDs that are being merged to form this new user. only set when merging users",
      "items": {
        "type": "string",
        "description": "user ID"
      }
    },
    "name": {
      "type": "string",
      "description": "name of the user"
    }
  },
  "required": ["name"]
}
